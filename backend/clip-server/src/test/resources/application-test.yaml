spring:
  config:
    import: "" # AWS Secrets Manager 차단

  datasource:
    url: jdbc:tc:mysql:8.0:///clip_db
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

# spring 밖에 두어야 하는 설정들 (Gemini 등)
gemini:
  api:
    url: https://test-url.com
    key: test-key-12345